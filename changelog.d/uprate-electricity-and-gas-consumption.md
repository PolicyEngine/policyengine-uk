- Uprate `electricity_consumption` and `gas_consumption`. Both variables declare CPI uprating, but were absent from `uprating_indices.yaml`, so they returned identical values in every year while every other consumption variable on the same index rose. This also restores the documented identity `domestic_energy_consumption == electricity_consumption + gas_consumption`, which held in the base year but diverged thereafter because only the combined variable was uprated.
